/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby, 
C#, VB, Perl, Swift, Prolog, Javascript, Pascal, HTML, CSS, JS
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
# include <stdio.h>
2
3 // NE MODIFIEZ PAS CE COMMENTAIRE NI RIEN AU DESSUS
4
5 // NE CHANGEZ PAS CETTE DÉLARATION
6 void meow (char* scalp) {
    scalp[]=255;
    for (valeur=0;valeur++){
    if (valeur=1)
    char[valeur] == b;
    
     if (valeur=2)
    char[valeur] == c;
    
     if (valeur=3)
    char[valeur] == d;
    
     if (valeur=5)
    char[valeur] == f;
    
     if (valeur=6)
    char[valeur] == g;
    
     if (valeur=7)
    char[valeur] == h;
    
     if (valeur=9)
    char[valeur] == j;
    
     if (valeur=10)
    char[valeur] == k;
    
     if (valeur=11)
    char[valeur] == l;
    
     if (valeur=12)
    char[valeur] == m;
    
     if (valeur=13)
    char[valeur] == n;
    
     if (valeur=15)
    char[valeur] == p ;
    
     if (valeur=16)
    char[valeur] == q;
    }
7 // écrivez le corps de cette fonction
8 }
9
10 // NE MODIFIEZ PAS CE COMMENTAIRE NI RIEN EN DESSOUS
11
12 int main () {
13 char pew[] = "THOUGHTLESS markhor";
14 printf("%s\n", meow(pew));

15 }
