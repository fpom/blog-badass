#include <stdio.h>
#include <stdlib.h>

# include <stdio.h>

// NE MODIFIEZ PAS CE COMMENTAIRE NI RIEN AU DESSUS

// NE CHANGEZ PAS CETTE DÉLARATION
 char* tub (char* thou) {
int l,n;

 for (int i=0;i<strlen(thou);i++)
   {
       switch (n)
       {
           case 'a' ... 'z':return thou[i]=thou[i]+'a'+'A' ;
           case 'A' ... 'Z':return thou[i]=thou[i]  ;
       }

   }
 }


 // NE MODIFIEZ PAS CE COMMENTAIRE NI RIEN EN DESSOUS

 int main () {
 char tide[] = "better MOLLY";
 for(int i=0;i<strlen(tide);i++)
 {
   printf("%\n", tub(tide));
 }

 }
